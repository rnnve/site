# Agents

Next.js 16 app on Vercel (`rinnesite`). Source lives at the repo root: `app/`, `components/`, `lib/` (no `src/`).

```
bun install
bun dev
```

Docs: [Next.js App Router](https://nextjs.org/docs/app), [Tailwind CSS v4](https://tailwindcss.com/docs).
